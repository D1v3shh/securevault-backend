# Dependencies
node_modules/

# Build
dist/
build/

# Environment
.env
.env.local
.env.production

# Storage — ignore contents but keep directory structure
storage/*
!storage/uploads/
storage/uploads/*
!storage/uploads/.gitkeep
!storage/temp/
storage/temp/*
!storage/temp/.gitkeep

# Logs — ignore contents but keep directory structure
logs/*
!logs/.gitkeep

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test — coverage, reports, generated certs, scripts
coverage/

# Test certificates (generated PEMs with private keys — NEVER commit)
test-certificates/

# Test reports (auto-generated JSON reports)
test-reports/

# Test scripts (only needed on testing branch)
scripts/generate-test-certs.ts
scripts/run-api-tests.ts

# Dev testing module (only for local testing, not production)
src/modules/dev/

# Misc test files
testing.txt

# Docker
docker-data/
vault-data/
mongo-data/
redis-data/
docker-compose.yml