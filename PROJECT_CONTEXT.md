# SecureVault Backend — Project Context

> Reference document for engineers and AI agents continuing development on this repository.
> Generated by inspecting the codebase, running the build, the linter, and the test suite.
> Everything stated as fact was verified against source or command output. Uncertain items are marked **NEEDS VERIFICATION**.

**Repo root:** `/home/divesh-kumawat/College/Projects/SecureVault/Prototype_2/securevault-backend`
**Git:** branch `main` (`534ac53 Fixed error`), remotes: `main`, `features`, `linux_main`, `testing`

---

## 1. Project Purpose

SecureVault is the **backend for an enterprise secure file-sharing and device-trust platform**. It exists to let an organization:

- Onboard employee workstations through admin-issued **enrollment tokens**, issuing each device an **X.509 client certificate** signed by a HashiCorp Vault PKI intermediate CA.
- Allow the desktop client to log in **passwordlessly** by presenting that certificate (device fingerprint bound).
- Store files **encrypted at rest** using envelope encryption (per-file AES-256-GCM DEK wrapped by a Vault-held KEK).
- Share files user-to-user with expiry, permission levels, and enterprise controls (download caps, one-time access, device pinning).
- Enforce **RBAC** across five roles and record an **audit trail** of every security-relevant action.

This repository is **backend only**. There is no frontend in this workspace (see §5.6 for the sibling desktop app).

---

## 2. Architecture

Single NestJS 11 monolith, modular by feature, with provider interfaces at every infrastructure boundary so implementations can be swapped.

```
                       ┌─────────────────────────────┐
  SetupApp (Electron)  │  NestJS API  /api/v1        │
  Main desktop client ─▶  Helmet · CORS · Throttler  │
                       │  Global JwtAuthGuard        │
                       │  Global RolesGuard          │
                       │  ValidationPipe (whitelist) │
                       │  TransformInterceptor       │
                       │  GlobalExceptionFilter      │
                       └───┬────────┬──────────┬─────┘
                           │        │          │
                    MongoDB 7   Redis 7    Vault 1.15
                  (Mongoose 9)  (ioredis)  (KV + PKI + Transit)
                                              │
                                    Local disk ./storage/uploads
                                    (encrypted blobs)
```

### Request pipeline (order matters)

1. `helmet()`, `cookieParser()`, CORS (`src/main.ts`)
2. `ValidationPipe` — `whitelist: true`, `forbidNonWhitelisted: true`, error messages suppressed in production
3. Global guards, registered in `app.module.ts` via `APP_GUARD`: `JwtAuthGuard` → `RolesGuard` → `ThrottlerGuard`
4. Global interceptors: `LoggingInterceptor`, then `TransformInterceptor`
5. `GlobalExceptionFilter` catches everything

**Auth is deny-by-default.** Every route requires a JWT unless decorated `@Public()`.

### Response envelope

`TransformInterceptor` wraps every successful response:

```json
{ "statusCode": 200, "message": "Success", "data": { ... }, "timestamp": "..." }
```

Errors (from `GlobalExceptionFilter`):

```json
{ "statusCode": 403, "error": "Forbidden", "message": "...", "timestamp": "...", "path": "...", "method": "..." }
```

`GET /files/:id/download` bypasses the envelope — it writes the raw decrypted buffer via `@Res()`.

### Dependency-injection seams

| Token | Interface | Current implementation |
|---|---|---|
| `STORAGE_PROVIDER` | `IStorageProvider` | `LocalStorageProvider` (disk) |
| `ENCRYPTION_PROVIDER` | `IEncryptionProvider` | `EncryptionService` |
| `KEY_MANAGEMENT_PROVIDER` | `IKeyManagementProvider` | `EncryptionService` |
| `VAULT_PROVIDER` | `IVaultProvider` | `VaultService` |
| `REDIS_CLIENT` | — | `ioredis` instance (global module) |

Tokens live in `src/shared/constants/app.constants.ts` (`INJECTION_TOKENS`) and alongside each interface.

Global modules (no import needed): `RedisModule`, `VaultModule`, `AuditModule`.

---

## 3. Tech Stack

| Layer | Technology |
|---|---|
| Runtime | Node.js 20 (Dockerfile pins `node:20-alpine`) |
| Framework | NestJS 11 (`@nestjs/platform-express`) |
| Language | TypeScript 5.7, `module`/`moduleResolution`: `nodenext`, target ES2023 |
| Database | MongoDB 7 via Mongoose 9 |
| Cache / token blacklist | Redis 7 via `ioredis` |
| Secrets, PKI, transit | HashiCorp Vault 1.15 via `node-vault` |
| Auth | Passport (`passport-jwt`) + `@nestjs/jwt`, bcrypt (12 rounds) |
| Validation | `class-validator` + `class-transformer` (DTOs), `zod` (env) |
| Docs | `@nestjs/swagger` at `/api/docs` (non-production only) |
| Rate limiting | `@nestjs/throttler` |
| Logging | Nest built-in `Logger`; `winston` wrapper exists but is unwired (§10) |
| Tests | Jest 30 + `ts-jest`, `supertest` |

**TypeScript strictness is partial:** `strictNullChecks` and `noImplicitAny` are on, but full `strict` is **off**. `any` is used liberally in services.

### Dependency notes

- `package.json` contains two junk dependencies from accidental installs: **`"20": "^3.1.9"`** and **`"nvm": "^0.0.4"`**. Neither is imported anywhere. Safe to remove.
- `bullmq` and `@nestjs/bull` are installed but **never imported** — the queue module does not use them (§10).

---

## 4. Directory / File Responsibilities

```
src/
├── main.ts                     Bootstrap: prefix, helmet, CORS, pipes, filters, interceptors, Swagger
├── app.module.ts               Root wiring + global guards + super-admin seed in onModuleInit()
├── config/
│   ├── configuration.ts        Array of registerAs() namespaces loaded by ConfigModule
│   ├── env.validation.ts       Zod schema; app refuses to boot on invalid env
│   ├── app.config.ts           'app' namespace — reads version from package.json  ⚠ see §10.1
│   ├── database.config.ts      'database'   jwt.config.ts   'jwt'
│   ├── redis.config.ts         'redis'      vault.config.ts 'vault'
│   └── storage.config.ts       'storage'
├── common/
│   ├── dto/pagination.dto.ts           PaginationDto + PaginatedResponse<T>
│   ├── filters/http-exception.filter.ts GlobalExceptionFilter (stack only in development)
│   └── interceptors/                    LoggingInterceptor, TransformInterceptor
├── shared/
│   ├── constants/app.constants.ts  APP_CONSTANTS, QUEUE_NAMES, INJECTION_TOKENS, ALLOWED_MIME_TYPES
│   ├── logger/logger.service.ts    AppLoggerService (winston) — NOT wired up
│   └── utils/                      crypto.util.ts, file.util.ts, certificate.util.ts
└── modules/
    ├── database/    Mongoose forRootAsync (pool 2–10, autoIndex off in production)
    ├── redis/       @Global ioredis provider, lazyConnect + retryStrategy (gives up after 5)
    ├── vault/       VaultService (KV v2, transit) + VaultPkiService (sign/revoke/CA/CRL)
    ├── encryption/  EncryptionService — AES-256-GCM + envelope encryption + KEK rotation
    ├── storage/     StorageService facade + LocalStorageProvider (path-traversal guarded)
    ├── permissions/ Role enum, ROLE_HIERARCHY, Permission enum, ROLE_PERMISSIONS, PermissionsService
    ├── users/       UserEntity, RefreshTokenEntity, UsersService, /users/me controller
    ├── auth/        AuthService, guards (jwt, jwt-refresh, roles), strategies, decorators, DTOs
    ├── admin/       AdminService/Controller — user mgmt, enrollment tokens, devices, audit logs
    ├── setup/       EnrollmentTokenEntity + SetupService/Controller (SetupApp-facing, @Public)
    ├── certificates/ CertificateEntity, CertificateRevocationEntity, sign/verify/revoke/status
    ├── devices/     DeviceEntity + trust lifecycle (pending→approved→revoked/blocked)
    ├── sessions/    SessionEntity + SessionsService (created on certificate login only)
    ├── files/       FileEntity + upload/download/list/soft-delete with envelope encryption
    ├── shares/      FileAccessEntity, ShareService, FileAccessValidationService, mapper, exceptions, tests
    ├── audit/       AuditLogEntity, AuditService, AuditAction enum, AuditInterceptor (unwired)
    ├── queue/       AuditProcessor, FileProcessor — plain services, no BullMQ, no scheduler
    └── health/      GET /health (public), GET /health/detailed (authenticated)

scripts/setup-vault-pki.ts   11-step Vault bootstrap: root CA, intermediate CA, device role, transit
scripts/setup-transit.ts     Transit engine + securevault-key only (idempotent)
postman/                     Postman collection — covers auth, admin, setup, certificates, devices
test/app.e2e-spec.ts         Stale Nest scaffold, would fail; excluded from `npm test`
```

---

## 5. Major Features and Flows

### 5.1 Password login → tokens

`POST /api/v1/auth/login` (public, throttled 5/60s) → `AuthService.login()`

1. `usersService.findByEmail` (lowercased, `deletedAt: null`)
2. Reject if `!isActive` (403) or account locked (403)
3. `bcrypt.compare`; on failure `recordLoginAttempt(false)` → increments `failedLoginAttempts`, locks for **30 min** at **5** attempts (`APP_CONSTANTS`)
4. On success reset counters, stamp `lastLoginAt` / `lastLoginIp`
5. `generateTokens()` — access JWT (`type: 'access'`, 15m default) + refresh JWT (`type: 'refresh'`, 7d), signed with **separate secrets**
6. `storeRefreshToken()` — SHA-256 hash of the JWT stored in `refresh_tokens` (raw token never persisted)
7. Returns tokens plus `mustChangePassword` / `isFirstLogin` flags

Every authenticated request: `JwtStrategy.validate()` rejects non-`access` tokens, then checks Redis key `sv:bl:token:<jwt>`; if present → 401 "Token has been revoked". Request gets `req.user = { userId, uuid, email, role }`.

### 5.2 Device enrollment (SetupApp flow)

```
Admin: POST /admin/create-enrollment-token   (ADMIN|SUPER_ADMIN)
   └─ enr_<64 hex>, bound to userId + employeeId, expiresInHours (default 24), maxDevices (default 1)

Device: POST /setup/verify-token   (public)  → validity, remainingDevices, expiresAt
Device: POST /setup/enroll         (public, 5/60s)
   1. verifyToken()  — expiry, usedCount < maxDevices, employeeId match, user active
   2. CertificateUtil.isValidCsrPem(csr)                       → 400 if malformed
   3. Duplicate fingerprint check → BLOCKED = 400, APPROVED = 409
   4. devicesService.registerDevice()  → status PENDING
   5. devicesService.approveDevice()   → APPROVED, approvedBy 'system:enrollment'
   6. certificatesService.signAndStoreCertificate()
        CN = <employeeId>.<deviceId>.securevault.local, ttl 8760h
        VaultPkiService.signCsr → pki_int/sign/securevault-device
        persists certificate + csr PEM, SHA-256 fingerprint, validity window
        devicesService.bindCertificate(deviceId, serial)
   7. Consume token (usedCount++, record fingerprint + IP, isUsed when exhausted)
   8. Audit: DEVICE_ENROLLMENT
   → { certificate, serialNumber, issuingCa, caChain, deviceId, validFrom, validTo }
```

**Enrollment auto-approves the device.** The token is treated as the admin's trust decision; there is no separate approval step in this path.

### 5.3 Certificate login (passwordless)

`POST /api/v1/auth/certificate-login` (public, 10/60s) → `certificateLogin()`

`CertificatesService.verifyCertificate()` runs ten checks in order: PEM format → parse via `crypto.X509Certificate` → `validTo` → `validFrom` → lookup by serial (falls back to a **full-collection scan with normalized serial comparison**) → `status !== REVOKED` → no row in `certificate_revocations` → device fingerprint matches the stored binding → `devicesService.isDeviceTrusted(fingerprint)` (must be `APPROVED`) → stored SHA-256 fingerprint matches recomputed.

Chain verification against the intermediate CA is attempted but **failures only log a warning** — deliberately soft so mock dev certificates pass. Comment in source says this must become a hard failure in production.

On success: issue JWTs → store hashed refresh token → `sessionsService.createSession(authMethod: 'certificate')` (ends prior sessions for that device) → `devicesService.updateLastSeen` → audit `CERT_LOGIN_SUCCESS`.
On failure: audit `CERT_LOGIN_FAILURE`, plus `REVOKED_CERT_LOGIN` or `FINGERPRINT_MISMATCH` security events, then 401.

### 5.4 File upload / download (envelope encryption)

**KEK initialization** (`EncryptionService.onModuleInit`): read `secret/data/securevault/master-encryption-key` from Vault → if absent and Vault healthy, generate 32 random bytes and write it → otherwise **fall back to `sha256(JWT_ACCESS_SECRET)`** with a loud warning.

Upload — `POST /files/upload` (multipart, `FileInterceptor('file')`, 100 MB cap in both Multer and `ParseFilePipe`):

1. MIME allow-list check (`APP_CONSTANTS.ALLOWED_MIME_TYPES`, 21 types) → 400
2. SHA-256 checksum of the **plaintext** buffer
3. `generateKey()` → fresh DEK; `encryptKey()` wraps it as `[iv(16)][authTag(16)][ciphertext]`; encrypted DEK also mirrored into Vault at `securevault/file-keys/<keyId>`
4. `encrypt(buffer, dek)` → AES-256-GCM
5. Storage path `YYYY/MM/DD/<uuid><ext>`; encrypted bytes written to disk
6. Mongo `files` doc holds `encryptedDek`, `encryptionIv`, `encryptionAuthTag` (all hex), checksum, `accessLevel`
7. Audit `FILE_UPLOAD`

Download — `GET /files/:id/download`: access check → read blob → `decryptKey` → `decrypt` → **recompute SHA-256 and compare to stored checksum**; mismatch → 400 "File integrity check failed" → audit `FILE_DOWNLOAD` → stream with `Content-Disposition: attachment`, `no-store`, `nosniff`.

The whole file is buffered in memory on both paths. `downloadStream` exists on the provider but is unused.

**File access control** (`findFileWithAccessCheck`, resolves by `uuid` first then `_id`):

| accessLevel | Who can read |
|---|---|
| `private` | owner, ADMIN, SUPER_ADMIN |
| `department` | owner, ADMIN, SUPER_ADMIN, MANAGER |
| `internal`, `public` | any authenticated user |

Delete and any `ownerOrAdminOnly` operation: owner or ADMIN/SUPER_ADMIN only. Delete is **soft** (`isDeleted`, `deletedAt`, `deletedBy`).

### 5.5 File sharing

`POST /shares` → `ShareService.shareFile()` validates: file exists and not deleted → **requester owns the file (read from DB, never trusted from the request)** → target user exists, not soft-deleted, active → not self → `expiresAt` parses and is in the future → no existing ACTIVE share for the same (file, user). A partial unique index on `{fileId, sharedWithUserId, status}` filtered to `ACTIVE` enforces the last rule at the database level too.

`DELETE /shares/:shareId` — owner only, sets `REVOKED`.
`GET /shares/shared-with-me`, `GET /shares/shared-by-me` — paginated, default filter `status = ACTIVE`, sortable by `createdAt` / `expiresAt`; `search` switches to an aggregation pipeline that `$lookup`s `files` and re-fetches each row to populate.
`GET /shares/:shareId` — owner or recipient only.

Permission model (`SharePermission` VIEW 10 < DOWNLOAD 20 < EDIT 30 < FULL_ACCESS 100; `ShareAction` maps DELETE and SHARE to FULL_ACCESS).

`FileAccessValidationService` implements the enforcement side: active-share lookup, **lazy expiration** (flips `ACTIVE`→`EXPIRED` on access and audits it), permission check, download-cap check, device-certificate pinning, plus `recordDownload()` (one-time access → `REVOKED`; cap reached → `EXPIRED`) and `expireOverdueShares()` for a future scheduled job.

> ⚠ **This service is never called.** `FilesService.downloadFile` does not consult shares. See §10.2.

### 5.6 Desktop clients (outside this repo)

`../setupapp/` is a separate git repository: Electron client (`electron-vite`, `node-forge`, `node-machine-id`, `electron-store`) plus its own **Fastify** enrollment server. `setupapp/client/src/renderer/src/services/api.ts` points at `http://localhost:3500/api/v1/enrollment` — i.e. the Electron client currently talks to the Fastify server, **not** to this NestJS backend's `/api/v1/setup/*` endpoints.

**NEEDS VERIFICATION:** whether the SetupApp is meant to be repointed at this backend, or whether the Fastify server proxies to it. `../Resources/mTLS_integration_context.md` likely holds the intended design.

---

## 6. APIs and Database

### 6.1 Endpoints

Base path `/api/v1`. Auth required unless marked Public. `ADMIN+` means `@Roles(SUPER_ADMIN, ADMIN)`.

**Auth** (`auth.controller.ts`)

| Method | Path | Access | Notes |
|---|---|---|---|
| POST | `/auth/login` | Public | throttle 5/60s |
| POST | `/auth/certificate-login` | Public | throttle 10/60s |
| POST | `/auth/refresh` | Public | throttle 10/60s, rotates refresh token |
| POST | `/auth/logout` | JWT | blacklists access token, revokes refresh, ends all sessions |
| POST | `/auth/change-password` | JWT | revokes all refresh tokens |
| POST | `/auth/force-change-password` | JWT | requires `mustChangePassword`; returns fresh tokens |

**Users** — `GET /users/me`, `PATCH /users/me` (only `firstName`, `lastName`, `jobTitle` are applied).

**Admin** (all ADMIN+ unless noted): `POST /admin/create-user`, `POST /admin/users` (alias), `GET /admin/users`, `GET /admin/users/:id`, `PATCH /admin/users/:id`, `POST /admin/users/:id/activate`, `POST /admin/users/:id/deactivate`, `POST /admin/users/:id/reset-password`, `PATCH /admin/users/:id/role` (**SUPER_ADMIN only**), `POST /admin/create-enrollment-token`, `GET /admin/devices`, `GET /admin/audit-logs`.

**Setup** — all four are `@Public()`: `POST /setup/verify-token` (10/60s), `POST /setup/enroll` (5/60s), `POST /setup/generate-certificate` (3/60s — see §10.3), `POST /setup/renew-certificate` (3/60s).

**Certificates** — `POST /certificates/verify` (Public, 20/60s), `POST /certificates/revoke` (ADMIN+), `GET /certificates/:serial` (JWT), `GET /certificates/status/:serial` (JWT).

**Devices** — `POST /devices/register` (JWT), `GET /devices/me` (JWT), `GET /devices/:id` (JWT, **no ownership check** — see §10.4), `PATCH /devices/:id/status` (ADMIN+).

**Files** — `POST /files/upload`, `GET /files`, `GET /files/:id`, `GET /files/:id/download`, `DELETE /files/:id` (all JWT).

**Shares** — `POST /shares`, `DELETE /shares/:shareId`, `GET /shares/shared-with-me`, `GET /shares/shared-by-me`, `GET /shares/:shareId` (all JWT). **Not documented in README.md.**

**Health** — `GET /health` (Public, Mongo readyState only), `GET /health/detailed` (JWT, adds Redis ping, Vault seal status, uptime, memory).

### 6.2 MongoDB collections

| Collection | Entity | Key fields / indexes |
|---|---|---|
| `users` | `UserEntity` | unique `uuid`, unique lowercase `email`; `role`, `isActive`, `failedLoginAttempts`, `lockoutUntil`, `mustChangePassword`, `deletedAt` (soft delete). `toJSON` strips `passwordHash`/`temporaryPassword`. Virtual `fullName`. |
| `refresh_tokens` | `RefreshTokenEntity` | unique `token` (**SHA-256 hash**), `userId`, `isRevoked`, TTL index on `expiresAt` (expireAfterSeconds 0) |
| `files` | `FileEntity` | unique `uuid`, `storagePath`, `checksum`, `encryptionKeyId`, `encryptedDek`, `encryptionIv`, `encryptionAuthTag`, `uploadedBy`, `accessLevel` (`private`\|`internal`\|`department`\|`public`), soft delete |
| `file_access` | `FileAccessEntity` | `fileId`, `ownerId`, `sharedWithUserId`, `permission`, `status`, `sharedAt`, `expiresAt`, `maxDownloads`, `downloadCount`, `oneTimeAccess`, `watermarkEnabled`, `allowedDeviceCertificateId`; **partial unique index** on `{fileId, sharedWithUserId, status}` where `status = ACTIVE` |
| `devices` | `DeviceEntity` | unique `deviceId` (uuid), unique `fingerprint`, `status` (pending/approved/revoked/blocked), `certificateSerial`, `lastSeenAt/Ip`, approval + revocation audit fields |
| `certificates` | `CertificateEntity` | unique `serialNumber`, unique `uuid`, `deviceId`, `deviceFingerprint`, `fingerprint` (SHA-256 of PEM), `certificatePem`, `csrPem`, `status`, `validFrom/validTo`, `renewalCount`, `previousSerial` |
| `certificate_revocations` | `CertificateRevocationEntity` | unique `certificateSerial`, RFC 5280 `reason` enum, `revokedBy`, `revokedAt`. Append-only by design |
| `enrollment_tokens` | `EnrollmentTokenEntity` | unique `token`, `userId`, `employeeId`, `expiresAt`, `maxDevices`, `usedCount`, `isUsed`, TTL index (expiry + 30 days) |
| `sessions` | `SessionEntity` | unique `sessionId`, `userId`, `deviceId`, `certificateSerial`, `isActive`, `authMethod`, TTL on `endedAt` (30 days, partial index) |
| `audit_logs` | `AuditLogEntity` | `action`, `resource`, `resourceId`, `userId`, `userEmail`, `userRole`, `ipAddress`, `userAgent`, `metadata`, `status`, `timestamp`. `timestamps: false`. TTL index **commented out** |
| `permissions` | `PermissionEntity` | unique `{role, resource, action}` — DB override table for `ROLE_PERMISSIONS`. Currently unused at runtime |

`autoIndex` is **on** in development and **off** in production, so indexes must be created explicitly before a production deploy.

### 6.3 RBAC

`Role` enum with `ROLE_HIERARCHY` weights: `SUPER_ADMIN` 100 → `ADMIN` 80 → `MANAGER` 60 → `EMPLOYEE` 40 → `VIEWER` 20.

Two independent mechanisms:

- **`RolesGuard`** — plain `requiredRoles.includes(user.role)`, **exact match, no hierarchy**. `@Roles(ADMIN)` does *not* admit `SUPER_ADMIN`. Every existing admin route lists both explicitly, so this is currently correct but is a trap for new routes.
- **`AdminService.validateRoleChange`** — *does* use `ROLE_HIERARCHY`: an admin may only assign a role strictly below their own. Plus guards against self-deactivation and self-role-change, and role changes are SUPER_ADMIN-only.

`Permission` enum + `ROLE_PERMISSIONS` matrix and `PermissionsService` exist but **nothing calls them**; `hasHigherOrEqualRole`, `ADMIN_ROLES`, `USER_MANAGEMENT_ROLES` are also unused.

---

## 7. Configuration / Setup

### 7.1 Environment variables

`ConfigModule.forRoot({ isGlobal: true, load: configModules, validate: validateEnv, expandVariables: true })`. The Zod schema in `src/config/env.validation.ts` runs at boot and **throws with a field-by-field report** on failure. All values arrive as strings and are parsed inside the `registerAs` factories.

Required with no default: **`MONGODB_URI`**, **`JWT_ACCESS_SECRET`** (min 32 chars), **`JWT_REFRESH_SECRET`** (min 32 chars).

Groups (see `.env.example` for the full template): app (`NODE_ENV`, `APP_PORT` 3000, `APP_HOST` 0.0.0.0, `API_PREFIX` api/v1, `APP_CORS_ORIGINS` comma-separated) · mongo · redis (`REDIS_KEY_PREFIX` default `sv:`) · jwt (`15m` / `7d`) · vault (`VAULT_ENABLED`, `VAULT_ADDR`, `VAULT_TOKEN`, `VAULT_MOUNT_PATH` secret, `VAULT_SECRETS_PATH` securevault, `VAULT_TRANSIT_ENGINE`, retry) · vault PKI (`VAULT_PKI_PATH` pki_int, `VAULT_PKI_ROLE` securevault-device) · storage (`STORAGE_TYPE` local, paths, 100 MB) · encryption (`aes-256-gcm`, key 32, **IV 16**, tag 16) · rate limit (`RATE_LIMIT_TTL` 60, `RATE_LIMIT_MAX` 100; the `*_AUTH_*` vars are validated but **never read** — per-endpoint limits are hardcoded in `@Throttle` decorators) · logging · `SEED_SUPER_ADMIN_*` · `BULL_QUEUE_PREFIX` (unused).

Config namespaces read by key path: `app.*`, `database.*`, `jwt.*`, `redis.*`, `vault.*`, `storage.*`. Some services read **raw env names** instead (`VAULT_PKI_PATH`, `ENCRYPTION_*`, `RATE_LIMIT_*`, `REDIS_KEY_PREFIX`) — both styles work because Zod's parsed output is merged into the config source.

### 7.2 Local setup

```bash
npm install
docker compose up -d                       # mongo:27017, redis:6379, vault:8200
cp .env.example .env                       # then set the two JWT secrets (32+ chars each)
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"   # run twice
npx ts-node scripts/setup-vault-pki.ts     # root CA, intermediate CA, device role, transit
npm run start:dev
```

On first boot `AppModule.onModuleInit()` seeds the super admin from `SEED_SUPER_ADMIN_EMAIL` / `_PASSWORD` (idempotent; logs a warning if unset). Swagger at `http://localhost:3000/api/docs`.

Vault runs in **dev mode** (root token `dev-root-token`, in-memory) — re-run the PKI script after `docker compose down -v`.

`scripts/setup-vault-pki.ts` performs **11** steps (root CA → URLs → intermediate CA → CSR → sign → set-signed → URLs → `securevault-device` role → transit engine → `securevault-key`). README says 9 and omits the transit steps.

### 7.3 Scripts

`build` · `start` / `start:dev` / `start:debug` / `start:prod` · `lint` (**runs `eslint --fix`, which rewrites files**) · `format` · `test` / `test:watch` / `test:cov` / `test:e2e`.

---

## 8. Deployment

**Dockerfile** — two-stage. Builder: `npm ci` → `npm run build`. Production: `node:20-alpine`, `npm ci --only=production`, copies `dist/`, creates `storage/uploads`, `storage/temp`, `logs`, runs as non-root `nestjs:nodejs` (uid/gid 1001), `EXPOSE 3000`, `HEALTHCHECK` on `/api/v1/health`, `CMD ["node", "dist/main.js"]`.

**docker-compose.yml** — dev stack: the backend image plus `mongo:7`, `redis:7-alpine` (`--requirepass securevault_redis_dev`), `hashicorp/vault:1.15` (dev mode) on bridge network `securevault-net`; `./storage` and `./logs` bind-mounted; service-name env overrides for `MONGODB_URI`, `REDIS_HOST`, `VAULT_ADDR`.

**CI/CD** — `.github/workflows/deploy.yml`, on push/PR to `main`:

1. `build-and-test` — Node 20, `npm ci`, `npm run lint`, `npm run test`
2. `build-and-push-docker` — Docker Hub login, build and push `<user>/securevault:latest` (tagged only `latest`, no SHA)
3. `deploy-to-server` — SSH to a VPS, `docker-compose -f docker-compose.prod.yml pull/up -d --no-deps securevault-backend`, `docker image prune -f`

Secrets used: `DOCKERHUB_USERNAME`, `DOCKERHUB_TOKEN`, `SERVER_HOST`, `SERVER_USERNAME`, `SERVER_SSH_KEY`.

> **The pipeline cannot currently pass — both the lint step and the Docker build fail.** See §10.1. Also `docker-compose.prod.yml` referenced by the deploy step **does not exist in this repository** — presumably it lives on the VPS. **NEEDS VERIFICATION.**

**Production considerations not yet handled:** `autoIndex` is disabled in production (indexes must be created manually), Swagger is correctly disabled in production, storage is local disk only (no S3 provider), and the app trusts `req.ip` without `app.set('trust proxy', ...)` — behind a reverse proxy every audit log will record the proxy IP.

---

## 9. Security Considerations

**Implemented and verified**

- bcrypt 12 rounds; password complexity enforced on change (`@Matches` upper + lower + digit + special)
- Separate access/refresh secrets, distinct `type` claim, rejected if swapped
- Refresh tokens stored **SHA-256 hashed**; rotation on every refresh (old row `isRevoked = true`); TTL index cleans up
- Access-token blacklist in Redis with TTL equal to remaining token life; checked on every request in `JwtStrategy`
- Password change / reset / deactivation revoke **all** refresh tokens for that user
- Account lockout: 5 failures → 30 minutes; auto-clears on expiry
- Envelope encryption: per-file random DEK, AES-256-GCM with auth tag, KEK from Vault
- Post-decryption checksum verification (tamper/corruption detection)
- Path-traversal guard in `LocalStorageProvider.getFullPath`; `FileUtil.sanitizeFilename` strips to `[a-zA-Z0-9.\-_]`
- MIME allow-list, 100 MB cap
- Regex-injection escaping on every user-supplied search term (users, files, devices, shares)
- `ValidationPipe` with `whitelist` + `forbidNonWhitelisted`; error messages suppressed in production
- Helmet, explicit CORS origin list, `GlobalExceptionFilter` never leaks stacks outside development
- Share creation reads ownership from the database, never from the request body
- Audit log is append-only in practice (no update paths); `CryptoUtil.secureCompare` available for constant-time comparison

**Gaps and risks**

1. **`POST /setup/generate-certificate` is `@Public()` and signs arbitrary CSRs with no enrollment token and no authentication.** Only a 3/60s throttle stands in front of it. It also passes `userId: 'system'` into `new Types.ObjectId('system')`, which throws `BSONError` — verified — so it returns 500 rather than issuing a certificate. It is a latent unauthenticated certificate-issuance endpoint. Remove it or gate it behind `@Roles(SUPER_ADMIN, ADMIN)`.
2. **Certificate chain verification is advisory.** `verifyCertificate` step 10 logs a warning and continues when `verify()` fails. With `VAULT_ENABLED=false` the PKI service returns fake PEM "certificates" that `crypto.X509Certificate` cannot even parse, so `parseCertificate` returns null and login fails — meaning **certificate login only works with Vault enabled**. Before production, make chain failure a hard 401.
3. **KEK fallback derives the master key from `JWT_ACCESS_SECRET`** when Vault is unavailable. Files encrypted under the fallback key become undecryptable once Vault comes online (and vice versa), and rotating the JWT secret would destroy them. Never allow this path in production.
4. **`VaultService` uses a static root token** from env. No AppRole, no token renewal, no lease handling.
5. **AES-GCM IV is 16 bytes** (`ENCRYPTION_IV_LENGTH=16`) rather than the recommended 12. Functional, slightly off-spec. Changing it would break decryption of existing files.
6. **`.env.production.example` contains real-looking secrets** — two concrete 64-hex JWT secrets, a personal email, and a plaintext password (`DiKu@671`). It is currently **untracked** (`git status` shows `??`). Do not commit it; rotate those values.
7. **No `trust proxy`** → IP-based audit trails and throttling are wrong behind a load balancer.
8. **Files are fully buffered in memory** on upload and download; a few concurrent 100 MB transfers can exhaust the heap.
9. **`GET /devices/:id` has no ownership or role check** — any authenticated user can read any device's fingerprint, MAC, serial, and certificate serial by ID.
10. **`GET /files/:id/download` is not rate limited** beyond the global 100/60s, and `accessLevel: internal`/`public` files are readable by every authenticated user including `VIEWER`.
11. **Audit logs have no retention policy** — the TTL index is commented out and no export path exists.
12. Logout ends **all** the user's sessions across every device, not just the current one.

---

## 10. Known Issues / Technical Debt

### 10.1 Build and CI are broken (verified by running them)

- **`npm run build` fails.** `nest build` reports two errors:
  ```
  src/modules/audit/audit.service.ts:72:22 - error TS18046: 'filter.timestamp' is of type 'unknown'.
  src/modules/audit/audit.service.ts:73:20 - error TS18046: 'filter.timestamp' is of type 'unknown'.
  ```
  Cause: `filter` is typed `Record<string, unknown>`, so `filter.timestamp.$gte = ...` is not assignable. Fix by typing the date filter explicitly (e.g. `const ts: Record<string, Date> = {}`).
  Note `nest-cli.json` sets `deleteOutDir: true`, so a failed build still **wipes `dist/`**.

- **`npm run lint` fails** with 11 errors and 336 warnings. All 11 errors are `@typescript-eslint/require-await` (async methods with no `await`) in `jwt-refresh.strategy.ts`, `encryption.service.ts` (×4), `health.controller.ts`, `local-storage.provider.ts`, and `shares.integration.spec.ts` (×4). Since CI runs the lint step, **job 1 of the pipeline fails today**. Also note the script includes `--fix`, so running it locally rewrites source files.

- **`src/config/app.config.ts` crashes the app when compiled.** It does
  `readFileSync(join(__dirname, '..', '..', '..', 'package.json'))`. From `dist/config/` that resolves to `<repo-parent>/package.json`, which does not exist — verified by loading the compiled module, which threw `ENOENT: ... /Prototype_2/package.json`. The **committed `dist/` contains a different, fixed implementation** (`resolveAppVersion()` walking up until it finds a `package.json`) that loads fine. No branch (`main`, `features`, `linux_main`, `testing`) contains that fix in `src/`. **`dist/` is ahead of `src/`; rebuilding will regress it.** Port `resolveAppVersion` back into `src/config/app.config.ts`.

- **Build output layout is wrong.** `tsconfig.build.json` excludes only `node_modules`, `test`, `dist`, and `*spec.ts` — not `scripts/`. Including `scripts/*.ts` pushes the inferred `rootDir` up, so the build emits `dist/src/main.js` and `dist/scripts/…` (observed during the failed build) while `start:prod` runs `node dist/main` and the Dockerfile runs `node dist/main.js`. Add `"scripts"` to the exclude list.

### 10.2 Dead or unwired code

| Item | Status |
|---|---|
| `FileAccessValidationService` | Fully implemented, exported, **never called**. Shares can be created and listed but grant no actual file access — `FilesService.downloadFile` doesn't consult `file_access`. **This is the single biggest functional gap.** |
| `AuditInterceptor` | Provided and exported by `AuditModule`, never registered as a global or per-controller interceptor. All audit entries come from explicit `auditService.log()` calls. |
| `AppLoggerService` (winston) | Never instantiated. `main.ts` uses `bufferLogs: true` but never calls `app.useLogger()`, so the default Nest logger is used and `LOG_LEVEL` / `LOG_DIR` have no effect. `nest-winston` is installed and unused. |
| `QueueModule` | `AuditProcessor` and `FileProcessor` are plain injectables. `bullmq` / `@nestjs/bull` are installed but never imported, and **nothing schedules `cleanupExpiredFiles()` or `verifyFileIntegrity()`** — soft-deleted blobs are never actually removed from disk. |
| `JwtRefreshStrategy` / `JwtRefreshGuard` | Registered but unused; `/auth/refresh` is `@Public()` and validates the token manually in `AuthService`. |
| `PermissionsService`, `Permission`, `ROLE_PERMISSIONS`, `permissions` collection | Documentation-only; no runtime consumer. |
| `hasHigherOrEqualRole`, `ADMIN_ROLES`, `USER_MANAGEMENT_ROLES` | Unused. |
| `AuditAction.LOGIN_SUCCESS` / `LOGIN_FAILURE` / `LOGOUT` / `TOKEN_REFRESH` | Defined but never emitted — **password login, logout, and refresh are not audited**, only certificate login is. |
| `CertificateUtil.certificateMatchesCsr` | Stub — returns `certPubKeyHash.length > 0`, i.e. effectively always `true`. Comment says it needs `@peculiar/x509` or `node-forge`. |
| `StorageService.downloadStream` | Implemented, never used. |
| `test/app.e2e-spec.ts` | Stale scaffold expecting `GET /` → `"Hello World!"`. Would fail; excluded because `jest.config.js` `testRegex` only matches `src/**/*.spec.ts`. |
| `SessionsService` | Sessions are created only on certificate login. Password logins leave no session row, yet logout calls `endAllUserSessions`. `updateActivity` is never called. |

### 10.3 Correctness bugs

- `POST /setup/generate-certificate` → `new Types.ObjectId('system')` throws `BSONError` (verified) → 500.
- `CertificatesService.verifyCertificate` falls back to **loading every non-revoked certificate** and comparing normalized serials in application code when the exact-serial lookup misses. O(n) and unbounded — will degrade badly. Store a normalized serial column and index it.
- `ShareService.listShares` with `search` re-queries the database once per result row to populate refs (N+1 on top of the aggregation).
- `ShareSortField.FILE_NAME` silently falls back to `createdAt` in the non-search path.
- `FileAccessValidationService.validateAccess` receives `fileId`/`userId` as strings and queries ObjectId fields. Mongoose casts valid 24-hex strings, but a **file UUID** would throw a `CastError`. Relevant once the service is actually wired in.
- `RolesGuard` performs exact role matching despite `ROLE_HIERARCHY` existing — a `@Roles(Role.ADMIN)` route would lock out `SUPER_ADMIN`. All current routes list roles explicitly, so nothing is broken today.
- `MANAGER` and `VIEWER` have no endpoints gated to them; `MANAGER` only appears in the `department` file-access branch.
- README drift: it documents `/setup/generate-certificate` as nonexistent, describes the PKI script as 9 steps (it's 11), claims RBAC hierarchy inheritance the guard doesn't implement, and omits the entire `/shares` API.
- `package.json` contains bogus dependencies `"20"` and `"nvm"`.
- `.env.production.example` has `NODE_ENV=development` and `JWT_ACCESS_EXPIRATION=24h` despite the name.

### 10.4 Test coverage

`npx jest` → **3 suites, 44 tests, all passing** (~4 s). Every test is in `src/modules/shares/__tests__/` (`share.service.spec.ts`, `file-access-validation.service.spec.ts`, `shares.integration.spec.ts`) and uses mocked Mongoose models. **Auth, encryption, files, certificates, devices, setup, and admin have zero tests.** There is no integration test against a real MongoDB/Redis/Vault.

---

## 11. MUST KNOW / DO NOT BREAK

Rules for anyone — human or agent — changing this codebase.

### Cryptography and data integrity

1. **Never change `ENCRYPTION_ALGORITHM`, `ENCRYPTION_KEY_LENGTH`, `ENCRYPTION_IV_LENGTH`, or `ENCRYPTION_AUTH_TAG_LENGTH` on a system with existing files.** `decryptKey` slices the packed blob at fixed offsets `[iv(16)][authTag(16)][ciphertext]`; changing lengths makes every stored DEK — and therefore every file — permanently unreadable.
2. **Never change how the master KEK is derived or where it is stored** without a migration that re-wraps every `encryptedDek` in the `files` collection. Rotating `JWT_ACCESS_SECRET` while the Vault fallback path is in use destroys all file data.
3. **`EncryptionService.rotateKey()` only replaces the in-memory KEK and the Vault copy — it does NOT re-encrypt existing DEKs**, despite the docstring. Calling it in production orphans every existing file. Do not call it until re-wrapping is implemented.
4. **Keep the post-decryption checksum comparison** in `downloadFile`. It is the only tamper/corruption detection in the system.
5. **Never store a raw refresh token.** Always `sha256(token)` before writing to or reading from `refresh_tokens`.

### Auth and authorization

6. **`JwtAuthGuard` is global.** Adding a controller automatically protects it. Adding `@Public()` removes *all* authentication — justify every use.
7. **Keep the `payload.type` check** in both JWT strategies. Without it a refresh token becomes a valid access token.
8. **Keep the Redis blacklist check** in `JwtStrategy.validate`. Removing it makes logout cosmetic.
9. **`RolesGuard` is exact-match, not hierarchical.** Always list every permitted role: `@Roles(Role.SUPER_ADMIN, Role.ADMIN)`. If you convert it to hierarchical matching, audit every existing `@Roles` decorator first.
10. **Never trust client-supplied ownership or identity.** `ShareService.shareFile` deliberately loads the file and compares `uploadedBy` against `req.user.userId`. `FilesService.findFileWithAccessCheck` is the single access gate for files — route all new file operations through it.
11. **Preserve the self-protection checks** in `AdminService`: no self-deactivation, no self-role-change, and `validateRoleChange`'s strict "below your own level" rule.

### Data model

12. **Soft deletes:** users use `deletedAt`, files use `isDeleted`. Every query must filter accordingly — `UsersService` uses `deletedAt: null`, `FilesService` uses `isDeleted: false`. Omitting the filter leaks deleted records.
13. **Do not update or delete `audit_logs` or `certificate_revocations`.** Both are append-only by design.
14. **Do not drop the partial unique index** on `file_access` (`{fileId, sharedWithUserId, status}` where `status = ACTIVE`) — it is the last line of defence against duplicate shares.
15. **`autoIndex` is off in production.** Any new index must be created explicitly during deployment.
16. IDs are dual-keyed: `files`, `devices`, `certificates`, and `sessions` each have a business UUID *and* a Mongo `_id`, and several lookups try both. Keep the `try { findById } catch {}` pattern when adding resolvers — an invalid ObjectId string throws.

### Process

17. **Never commit `.env` or `.env.production.example`.** The latter currently contains real secrets and is untracked — keep it that way.
18. **`npm run lint` runs `eslint --fix` and rewrites source files.** Use `npx eslint <paths>` (no `--fix`) when you only want to inspect.
19. **`npm run build` deletes `dist/` before compiling and currently fails.** The committed `dist/` holds a fix for `app.config.ts` that is missing from `src/` — back up `dist/` before building, and port `resolveAppVersion()` into `src/config/app.config.ts` before relying on a fresh build.
20. **Fix the two `audit.service.ts` type errors and the 11 `require-await` lint errors first** if you need CI or Docker to succeed. Nothing else in the pipeline can pass until then.
21. **Env changes go in two places:** the Zod schema in `src/config/env.validation.ts` *and* the relevant `src/config/*.config.ts` factory. The app will refuse to boot on an unrecognised-but-invalid value, and a variable missing from the schema is silently unavailable.
22. **Vault dev mode loses all data on container restart.** After `docker compose down -v`, re-run `npx ts-node scripts/setup-vault-pki.ts` or certificate signing and KEK loading will fall back to mock/derived values.
23. **Do not "fix" `verifyCertificate` step 10 into a hard failure without checking `VAULT_ENABLED`** in the target environment — with Vault disabled the mock CA PEMs will fail every chain check and lock out all certificate logins.
24. **Wiring `FileAccessValidationService` into the download path is the intended next step.** When you do it, remember `FilesService.findFileWithAccessCheck` must let owners and admins through *before* the share lookup, and `recordDownload()` must be called only after a successful transfer.
